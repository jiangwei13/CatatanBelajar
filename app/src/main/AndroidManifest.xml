<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="svd.xsu.ktxy">

    <application
        android:name="svd.xsu.ktxy.FQGD"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        tools:replace="android:dataExtractionRules,android:fullBackupContent"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CatatanBelajar">

        <!-- Splash Screen Activity (Launcher) -->
        <activity
            android:name="svd.xsu.ktxy.JNUS"
            android:exported="true"
            android:theme="@style/Theme.CatatanBelajar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name="svd.xsu.ktxy.GMFY"
            android:exported="false"
            android:theme="@style/Theme.CatatanBelajar" />

        <!-- Detail Activity -->
        <activity
            android:name="svd.xsu.ktxy.JNUT"
            android:exported="false"
            android:theme="@style/Theme.CatatanBelajar" />

        <!-- Edit Activity -->
        <activity
            android:name="svd.xsu.ktxy.JNUU"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.CatatanBelajar" />

        <!-- Settings Activity -->
        <activity
            android:name="svd.xsu.ktxy.JNUV"
            android:exported="false"
            android:theme="@style/Theme.CatatanBelajar" />

        <!-- View All Activity -->
        <activity
            android:name="svd.xsu.ktxy.JNUW"
            android:exported="false"
            android:theme="@style/Theme.CatatanBelajar" />

    </application>

</manifest>